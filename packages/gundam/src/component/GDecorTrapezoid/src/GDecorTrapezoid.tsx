import { defineComponent, computed } from 'vue'

const GDecorTrapezoid = defineComponent({
  name: 'GDecorTrapezoid',
  setup(props, { slots }) {
    return () => <div>123</div>
  },
})
export default GDecorTrapezoid
